# Приложения (таблицы, расчёты, ссылки)

Крупные таблицы, рецепты, HS-статистика, сканы — сюда.  
В тематических `T*.md` — краткий текст + ссылка на файл в `appendix/`.

Пример:

```markdown
См. полная таблица рецептов: [appendix/feed-recipes-table.md](appendix/feed-recipes-table.md)
```
